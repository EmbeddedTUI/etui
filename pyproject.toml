[project]
name = "etui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "textual >= 1.0",
]

[project.scripts]
etui = "etui.main:main"